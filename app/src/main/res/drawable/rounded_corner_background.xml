<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/white" android:alpha="0" />
    <corners android:radius="20dp" /> <!-- chỉnh độ cong -->
    <stroke
        android:width="1dp"
        android:color="#FF0000" /> <!-- Màu xám -->
</shape>